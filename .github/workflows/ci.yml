name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      security-events: write
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install --frozen-lockfile

      # Human-readable errors appear in this step's log
      - name: Run ESLint
        id: eslint
        run: |
          set +e
          bunx eslint . --format stylish
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"

          # Also write SARIF for GitHub Security / code scanning
          bunx eslint . \
            --format=@microsoft/eslint-formatter-sarif \
            --output-file=eslint-results.sarif \
            || true

          if [ ! -f eslint-results.sarif ]; then
            echo "ESLint did not produce eslint-results.sarif"
            exit 1
          fi

      - name: Upload ESLint SARIF
        uses: github/codeql-action/upload-sarif@v4
        if: always() && hashFiles('eslint-results.sarif') != ''
        with:
          sarif_file: eslint-results.sarif
          category: eslint

      - name: Fail job on ESLint errors
        if: steps.eslint.outputs.exit_code != '0'
        run: |
          echo "::error::ESLint found problems. Open the 'Run ESLint' step above for the exact file/line/rule errors."
          exit 1
